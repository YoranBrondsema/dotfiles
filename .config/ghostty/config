shell-integration = zsh
copy-on-select = clipboard
right-click-action = copy
font-family = "JetBrains Mono"
# This disables contextual alternates, which includes the != → ≠ ligature.
font-feature = -calt
